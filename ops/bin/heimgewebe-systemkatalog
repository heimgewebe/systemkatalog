#!/usr/bin/env bash
set -Eeuo pipefail
ROOT="${SYSTEMKATALOG_ROOT:-$HOME/repos/cabinet}"
BIND="${SYSTEMKATALOG_BIND:-127.0.0.1}"
PORT="${SYSTEMKATALOG_PORT:-4001}"
exec /usr/bin/python3 "$ROOT/scripts/serve_system_catalog.py" --bind "$BIND" --port "$PORT"
